name: ETCCDI_precipitacion
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.10
  - cdo
  - numpy
  - scipy
  - matplotlib=3.7      # ← versión compatible con proplot
  - proplot=0.9.7       # ← última versión estable
  - netcdf4
  - pyyaml
  - pandas
  - pyproj
  - cartopy
  - jupyterlab
  - ipykernel
  - ipympl
  - pip:
    - tqdm
    - pymannkendall
