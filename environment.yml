name: ETCCDI_precipitacion
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.10
  - pip
  - cdo
  - numpy
  - scipy
  - matplotlib>=3.3,<3.5   # ← rango exacto que requiere proplot
  - netcdf4
  - pyyaml
  - pandas
  - pyproj
  - cartopy
  - jupyterlab
  - ipykernel
  - ipympl
  - pip:
    - proplot==0.9.7
    - tqdm
    - pymannkendall
